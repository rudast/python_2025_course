Проект командный
    сколько человек 4

    выполнения всех требований ниже - 8/9 при успешной защите

    если сверх требований - 10

    на что влияют плюсы

    человек с 5 плюсами остается без вопросов после рассказа и работе сервиса

    минусы или полное отсутствие + ведет к доп вопросам


из чего состоит демонстрация проекта

1 я хочу увидеть схему ваших сервисов miro
2 я хочу рассказ какую проблему вы решаете для кого и ....
3 скидываем qr код на телеграм бота
4 показываем как сервис работает в вебе
5 кто что делал ( для рубрики вопросов )

зимняя сессия

просьба к старостам создать страницу с темами группами и ссылками на репы





1. Обязательно используйте Docker
    Весь проект должен запускаться одной командой:

        ```bash
        docker-compose up

        ```

2 Каждая часть системы — в отдельном контейнере:
    - Веб-интерфейс (например, на FastAPI и streamlit ),
    - Telegram-бот,
    - База данных (PostgreSQL, SQLite  — на ваш выбор),
    - Worker (если есть — тоже в отдельном контейнере).(worker который обрабатывает запросы)

3 Как компоненты общаются между собой
    - Используйте REST API для связи между контейнерами.
    - kafka (amazing)  (если по уму ее настраивать, то это оч сложно)
    - grpc (amazing)
    Например:
    → Бот получает сообщение → делает HTTP-запрос к нейросетевому движку (worker) → получает ответ → отправляет его пользователю.
    → Веб-интерфейс может читать данные из той же базы или тоже обращаться к API движка.

    Важно:
    Не используйте общие файлы или костыли вроде записи в один том для обмена данными.

    наружу торчит только веб демо и телега. все остальные контейнеры недоступны.

    пользователь не мог сделать sql инъекцию и что-то подобное.

    Вся работа с кредами через .env по примеру из README


4 Надёжность и читаемость
    - Если что-то сломалось (нет интернета, БД не отвечает и т.п.), бот и веб не должны падать с ошибкой — ловите исключения!
    - Код должен быть понятным: названия функций по делу, немного комментариев, где логика нетривиальна.
    - В README.md напишите:
        Что делает ваш проект,
        Как запустить,
        Какие переменные нужны в .env (с примерами).


🛡 Безопасность (минимум)
    - Не выводите токены и пароли в логи (print, logging).
    - Базу данных не надо выставлять наружу — только для других контейнеров внутри Docker.
    - Веб-сервер может быть доступен с хоста (для тестов), но без лишних рисков (например, без отладочного режима в продакшене)Обязательно используйте Docker
