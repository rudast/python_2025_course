
## 🐳 Часть 1. Базовые команды Docker (CLI)

### 🔹 1. Контейнеры

| Задача | Команда | Пояснение |
|-------|--------|----------|
| Посмотреть **запущенные** контейнеры | `docker ps` | Только активные |
| Посмотреть **все** контейнеры (включая остановленные) | `docker ps -a` | `-a` = all |
| Остановить контейнер | `docker stop <container_id или имя>` | Корректная остановка |
| Удалить контейнер | `docker rm <container_id или имя>` | Только остановленный |
| Удалить **все остановленные** контейнеры | `docker container prune` | Подтверждение: `y` |
| Запустить контейнер в фоне | `docker run -d --name myname image` | `-d` = detached |
| Посмотреть логи контейнера | `docker logs <имя>` | Без `-f` — только прошлые |
| Логи в реальном времени | `docker logs -f <имя>` | `-f` = follow |

---

### 🔹 2. Образы (images)

| Задача | Команда |
|-------|--------|
| Посмотреть все образы | `docker images` |
| Удалить образ | `docker rmi <image_id или имя>` |
| Удалить **неиспользуемые** образы | `docker image prune -a` |

---

### 🔹 3. Сети (networks)

| Задача | Команда | Пояснение |
|-------|--------|----------|
| Посмотреть все сети | `docker network ls` | |
| Посмотреть детали сети | `docker network inspect <network_name>` | Покажет, какие контейнеры в ней |
| Создать сеть | `docker network create mynet` | Редко нужно вручную |
| Удалить сеть | `docker network rm <network_name>` | Только если не используется |

> 💡 По умолчанию у контейнера есть сеть `bridge`. При использовании `docker-compose` создаётся своя сеть (например, `myproject_default`).

---

### 🔹 4. Тома / Volumes (постоянное хранилище)

| Задача | Команда |
|-------|--------|
| Посмотреть все volumes | `docker volume ls` |
| Посмотреть детали volume | `docker volume inspect <volume_name>` |
| Создать volume | `docker volume create myvol` |
| Удалить volume | `docker volume rm <volume_name>` |
| Удалить **неиспользуемые** volumes | `docker volume prune` |

> ⚠️ **Важно**: данные в volumes **не удаляются** при удалении контейнера — это и есть их смысл!

---

### 🔹 5. Прочие полезные команды

| Задача | Команда |
|-------|--------|
| Очистить всё неиспользуемое (осторожно!) | `docker system prune -a` |
| Посмотреть использование диска | `docker system df` |
| Зайти в запущенный контейнер | `docker exec -it <имя> sh` (или `bash`) |

---

## 🧩 Часть 2. Команды Docker Compose

> 💡 Docker Compose работает с файлом `docker-compose.yml` в текущей папке.

### 🔹 1. Управление сервисами

| Задача | Команда |
|-------|--------|
| Запустить всё в фоне | `docker compose up -d` |
| Пересобрать и запустить | `docker compose up --build -d` |
| Остановить всё | `docker compose down` |
| Остановить, но **сохранить volumes** | `docker compose down --volumes` → ❌ удаляет volumes! <br> Чтобы **сохранить** — просто `docker compose down` (volumes по умолчанию **не удаляются**) |
| Перезапустить один сервис | `docker compose restart <service_name>` |
| Пересобрать и перезапустить один сервис | `docker compose up --build -d <service_name>` |

---

### 🔹 2. Просмотр состояния (аналоги Docker CLI)

| Что хочешь увидеть? | Команда Docker Compose | Аналог в Docker |
|--------------------|------------------------|----------------|
| Активные контейнеры проекта | `docker compose ps` | `docker ps` (но только для этого проекта) |
| Логи всех сервисов | `docker compose logs` | `docker logs ...` для каждого |
| Логи одного сервиса | `docker compose logs <service>` | — |
| Сети проекта | ❌ **Нет прямой команды** | Используй `docker network ls` → ищи сеть вида `myfolder_default` |
| Volumes проекта | ❌ **Нет прямой команды** | Используй `docker volume ls` → ищи с префиксом папки |

> 💡 Docker Compose **не имеет отдельных команд** для просмотра сетей и volumes — потому что они создаются через Docker и управляются им же.

Но вы можете:
- Узнать имя сети проекта:
  ```bash
  docker compose config
  ```
  (и посмотреть секцию `networks`)

- Или просто:
  ```bash
  docker network ls
  ```
  → сети от Compose имеют имя: **`<папка>_default`**

  Пример: если вы в папке `myapp`, сеть будет называться `myapp_default`.

---

### 🔹 3. Полезные утилиты Compose

| Задача | Команда |
|-------|--------|
| Проверить синтаксис `docker-compose.yml` | `docker compose config` |
| Посмотреть имена сервисов | `docker compose config --services` |
| Выполнить команду в сервисе | `docker compose exec <service> sh` |
| Посмотреть переменные окружения сервиса | `docker compose exec <service> printenv` |

---

## 📌 Краткая сводка: что использовать?

| Нужно... | Используй |
|--------|----------|
| Посмотреть **все** контейнеры на машине | `docker ps -a` |
| Посмотреть контейнеры **только своего проекта** | `docker compose ps` |
| Посмотреть **все** сети | `docker network ls` |
| Посмотреть **свои** volumes | `docker volume ls` → ищи по префиксу папки |
| Перезапустить один сервис | `docker compose restart backend` |
| Полностью остановить проект | `docker compose down` |
| Очистить всё (осторожно!) | `docker system prune -a` |
